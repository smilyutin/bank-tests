# Security Test Report

##  Test Result: WARNING

**Test:** HSTS: Strict-Transport-Security header present on HTTPS
**Timestamp:** 2026-05-15T03:09:43.745Z
**OWASP Category:** API7:2023 - Server Side Request Forgery
**Risk Level:**  LOW

## Description

Environment limitation: HSTS validation could not run because the application is served over non-HTTPS URL (http://localhost:5001/).

## Why this result

Completed with warning because Environment limitation: HSTS validation could not run because the application is served over non-HTTPS URL (http://localhost:5001/).

##  Recommendations

1. Enforce HTTPS in all environments where security tests run.
2. Redirect all HTTP traffic to HTTPS before serving application content.
3. Only emit HSTS header on HTTPS responses after TLS is correctly configured.

---
*Generated by Security Test Reporter*
