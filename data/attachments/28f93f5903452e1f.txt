# Security Test Report

##  Test Result: WARNING

**Test:** Brute-force: repeated bad logins are throttled/locked
**Timestamp:** 2026-05-15T03:09:03.106Z
**OWASP Category:** API2:2023 - Broken Authentication
**Risk Level:**  LOW

## Description

True vulnerability: no brute-force throttling was detected. 6 failed login attempts to /api/login did not trigger 429 or lockout headers. Observed status progression: [500, 500, 500, 500, 500, 500]. Risk: an attacker can perform repeated password guessing with limited friction.

## Why this result

Completed with warning because True vulnerability: no brute-force throttling was detected. 6 failed login attempts to /api/login did not trigger 429 or lockout headers. Observed status progression: [500, 500, 500, 500, 500, 500]. Risk: an attacker can perform repeated password guessing with limited friction.

##  Recommendations

1. What to fix in the target app first (priority order):
2. 1) Add brute-force protection on login endpoints (per-IP and per-user limits)
3. 2) Return HTTP 429 with Retry-After when thresholds are exceeded
4. 3) Implement account lockout or progressive backoff after repeated failures
5. 4) Monitor and alert on repeated failed authentication attempts
6. Apply strict auth throttling policy on /api/login (e.g., 5-10 failed attempts per minute).
7. Use combined controls: per-user, per-IP, and global rate limits.
8. Add temporary lockout/CAPTCHA challenge after the threshold is exceeded.
9. Record auth-failure telemetry and feed SIEM detection rules.

---
*Generated by Security Test Reporter*
