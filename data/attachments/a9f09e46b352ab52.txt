# Security Test Report

##  Test Result: WARNING

**Test:** HSTS: max-age is sufficiently long
**Timestamp:** 2026-05-15T03:09:43.877Z
**OWASP Category:** API7:2023 - Server Side Request Forgery
**Risk Level:**  LOW

## Description

Environment limitation: HSTS max-age validation could not run because the application is served over non-HTTPS URL (http://localhost:5001/).

## Why this result

Completed with warning because Environment limitation: HSTS max-age validation could not run because the application is served over non-HTTPS URL (http://localhost:5001/).

##  Recommendations

1. Enforce HTTPS in all environments where security tests run.
2. Redirect HTTP to HTTPS and validate the TLS certificate chain.
3. Apply HSTS only after HTTPS is consistently enforced.

---
*Generated by Security Test Reporter*
